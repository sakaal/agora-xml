
Agora Exchange for Online Managed Services

XML schema generated JAXB classes

Maven 2 and Java 6 or later must be installed before building this module.

At the module root directory start the Maven build by typing:

mvn clean install

See the results under target/generated-sources/xjc/.
